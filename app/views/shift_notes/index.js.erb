<% js = escape_javascript(
  render(
    :partial => 'shift_notes/list',
    :locals => { :shift_notes => @shift_notes, current_user: @current_user, :access_level => @access_level.access_level }
  )
) %>
$('#results').html("<%= js %>");