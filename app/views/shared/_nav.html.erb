<nav class="navbar navbar-default navbar-fixed-top">

  <div class="container-fluid">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#vcms-primary-nav" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_url, class: 'navbar-brand' do -%>
        <%= image_tag('logo.png', class: 'img-responsive', alt: 'VCMS', srcset: "#{image_url('logo.png')} 1x, #{image_url('logo@2x.png')} 2x, #{image_url('logo@3x.png')} 3x") %>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="vcms-primary-nav">

      <ul class="nav navbar-nav">
        <li><a href="/">Home</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Apps <span class="caret"></span></a>
          <ul class="dropdown-menu">
          <!--
            <%#= primary_nav_link 'employee_notes', 'index', 'Employee Notes', true %>
            <%#= primary_nav_link 'shift_notes', 'index', 'Shift Notes', true %>
            <%#= primary_nav_link 'opto_messages', 'index', 'Opto Logs', true %>
            <%#= primary_nav_link 'documents', 'index', 'Documents', true %>
            <%#= primary_nav_link 'thickness', 'index', 'Thickness', true %>
            <%#= primary_nav_link 'salt_spray_tests', 'index', 'Salt Spray Tests', true %>
          -->
          </ul>
        </li>
      </ul>

      <% if current_user.nil? %>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to 'Login', login_url %></li>
        </ul>
      <% else %>
        <ul class="nav navbar-nav navbar-right">
          <% if current_user.is_sysadmin -%>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <%= primary_nav_link 'users', 'index', 'Manage Users' %>
                <%= primary_nav_link 'permissions', 'index', 'Manage Permissions' %>
              </ul>
            </li>
          <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.full_name %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Logout', logout_path, method: :delete %></li>
            </ul>
          </li>
        </ul>
      <% end %>

    </div>

  </div>

</nav>
